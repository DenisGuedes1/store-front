import { FormLogin } from "../../components/FormLogin";
import { ContainerForm } from "./style";

export const Login = () => {
  return (
    <ContainerForm>
      <FormLogin />
    </ContainerForm>
  );
};
